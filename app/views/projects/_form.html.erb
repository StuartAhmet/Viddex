
<div class="page-container pt-8">

  <%= simple_form_for [@user, @project] do |f| %>
  <div class="flex" style="justify-content:center;">
    <div class="box blue" style="width:600px; margin-top:40px">
    <h2>New Project</h2>
      <div class="flex" style="justify-content:space-between">
        <div >
            <%= f.association :background, collection: @user.backgrounds,  prompt: "Select background", label: false, :value => @user_backgrounds, as: :select, input_html: { multiple: false } %>
        </div>
        <div >
            <%= f.association :video, collection: @user.videos,  prompt: "Select video", label: false, :value => @user_videos, as: :select, input_html: { multiple: false } %>
        </div>
      </div>
      <div>
      <div class="flex"style="justify-content:space-between">
        <%= f.input :prospect_first, label: "Prospect first name", placeholder: "" %>
        <%= f.input :prospect_last %>
      </div>
      <div class="flex"style="justify-content:space-between">
        <div class="flex" style="flex-direction:column">
          <%= f.input :prospect_company %>
          <%= f.input :prospect_email %>
        </div>
        <%= f.input :message, placeholder:
        "Hi #{user.first_name.capitalize},
#{user.company_name.capitalize} / Videx" %>
      </div>
      </div>
      <div class="flex justify-start items-center pt-8">
        <div class="flex justify-center items-center h-10 rounded-lg bg-black" style="width:100%;border-radius:5px">
          <%= f.submit "Generate", class: "text-xl mx-8 text-black", style: "width:100%;border-radius:5px" %>
        </div>
      </div>
   </div>
  </div>
<% end %>
